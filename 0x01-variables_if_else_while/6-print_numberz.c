#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints all single digit numbers of base 10
 *
 * Return: always 0 (successful)
 */
int main(void)
{
	int v;
	{
	for (v = 48 ; v <= 57; v++)
		putchar(v);
	}
	putchar('\n');

	return (0);
}
