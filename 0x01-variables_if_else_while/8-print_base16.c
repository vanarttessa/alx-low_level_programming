#include <stdio.h>
/**
 * main - entry point in program
 *
 * Prints all the numbers of base 16 in lowercase,
 * followed by a new line.
 *
 * Return : 0 (success)
 */
int main(void)
{
	int x = 48;

	int y = 97;

	while (x < == 57; x++)
	{
		putchar(x);
	}
	while (y < == 122; y++)
	{
		putchar(y);
	}
	{
		putchar ('\n');
	}
	return (0);
}
