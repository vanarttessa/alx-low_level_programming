#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints all single digit numbers of base 10
 *
 * Return: always 0(successful)
 */
int main(void)
{
	int i;
	{
	for (i = 48; i <= 58; i++)
		putchar(i);
	}
	putchar('\n');

	return (0);
}
