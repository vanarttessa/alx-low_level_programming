#include "main.h"


/**
 * _abs - returning absoulte value of number
 *
 * @i: number to get absolute
 *
 * Return: absolute value of i
 */
int _abs(int i)
{

	if (i < 0)
	{
		return (-i);
	}
	return (i);
}
