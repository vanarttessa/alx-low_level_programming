#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Prints the last digit of a randomly generated number.
 *
 *Return : Alwaya 0 (sucessful)
 */
int main(void)
{
	int n
		printf("Last digit of %d\n");
		printf("is\n");
	/*If the last digit of %d is > 5 then print th following line*/
		{
		printf("and is greater than 5\n");
		}
	/*Else if %d == 0 then print the following line*/
		{
		printf("and is 0\n");
		}
	/*Else the last digit of %d is < 6 then print the following line*/
		{
			printf("and is less than 6 and not 0\n");
		}
		return (0);
}

