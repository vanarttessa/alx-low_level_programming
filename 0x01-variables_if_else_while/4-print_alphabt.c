#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
 * main - prints the alphabet in lowercase followed by a new line
 *
 * Return: always 0(successful)
 */
int main(void)
{
	int x;
	{
	for (x = 'A' ; x <= 'Z'; x++)
	{
	if ((x != 'E' && x != 'Q'; x++)
		putchar(tolower(x));
	}
	}
	putchar('\n');

	return (0);
}
